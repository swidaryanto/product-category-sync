{
  "name": "product-category-sync",
  "version": "1.0.0",
  "private": true,
  "description": "Monitors and syncs the Product Category tree in Google Sheets against the Cat Tree RM source tree, via Composio.",
  "type": "module",
  "scripts": {
    "check": "node scripts/check.js",
    "sync": "node scripts/sync.js"
  },
  "engines": {
    "node": ">=20"
  }
}
